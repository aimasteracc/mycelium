{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"agent-coordination","pattern":"Telling a subagent to 'WAIT for SendMessage from sibling X'","why-bad":"Subagents spawned via Agent tool are stateless one-shot workers with no inbox; they cannot wait; the workstream silently aborts","instead":"Use memory-as-bus: Lead reads sibling output from memory, then spawns the next agent with explicit memory keys as inputs"}
{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"agent-coordination","pattern":"Spawning N dependent agents in one batch expecting them to chain via peer messages","why-bad":"No peer messaging mechanism exists; agents run open-loop on stale assumptions","instead":"Spawn sequentially with Lead verifying upstream outputs in memory before each downstream spawn"}
{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"async","pattern":"Holding std::sync::Mutex across .await","why-bad":"Blocks the async reactor; can deadlock the entire runtime under load","instead":"Use tokio::sync::Mutex or refactor to release the lock before the await point"}
{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"tree-sitter","pattern":"Holding tree-sitter WASM heap across many files without recycling worker thread","why-bad":"WASM linear memory only grows, never shrinks; long-running indexes OOM","instead":"Recycle the parse worker thread every N=250 files (see codegraph prior art) and impose a per-file parse timeout"}
{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"resolution","pattern":"Unbounded HashMap caches in cross-file reference resolution","why-bad":"On 20k+ file repositories the caches consume gigabytes and OOM","instead":"All resolution caches must be LRU-bounded; content cache gets 1/5 the budget of metadata caches"}
{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"sqlite","pattern":"Adding single-column indexes when a composite index already covers the prefix","why-bad":"Single-column indexes become dead weight on writes and provide no read advantage when the composite (col, kind) is already present","instead":"Rely on left-prefix scan of the composite; drop redundant single-column indexes (see codegraph migration v4 prior art)"}
{"ts":"2026-05-28T00:00:00Z","agent":"founder","domain":"governance","pattern":"Letting any actor (human or AI) push directly to main or develop","why-bad":"Breaks branch protection, audit trail, and the registry-first release contract","instead":"All changes go through PR to develop; release/* and hotfix/* are the only paths to main"}
{"ts":"2026-05-28T00:00:00Z","agent":"founder","domain":"governance","pattern":"Adding language support by modifying core engine code","why-bad":"Violates Charter §4; each ad-hoc core change makes adding the next language harder","instead":"Express the new language entirely as files under packs/<lang>/ (pack.toml, queries.scm, optional hooks.wasm); if impossible, open an RFC to extend the pack interface itself"}

{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"data-modeling","lesson":"Two-phase extraction (parse then resolve) is essential for cross-file references. The first phase records unresolved references as data; the second phase, after all files are seen, promotes them to edges.","origin":"prior art: codegraph"}
{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"agent-coordination","lesson":"In a multi-agent system without peer messaging, 'memory-as-bus + lead-orchestrated phases' is the only reliable coordination pattern. The lead reads outputs from a shared store and spawns the next agent with explicit input keys.","origin":"hard-won across multiple prior agent system designs"}
{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"performance","lesson":"For a code graph queried by AI agents 90% of the time, the hot path is single-symbol lookup and 1-3 hop traversal. Optimize trunk lookup and synapse adjacency for these cases; do not over-engineer deep graph algorithms.","origin":"benchmark analysis of codegraph and Neo4j"}
{"ts":"2026-05-28T00:00:00Z","agent":"architect","domain":"ai-serialization","lesson":"AI consumers benefit massively (3-4x) from compact symbolic serialization (Emmet-style) vs JSON. Token efficiency is a first-class design constraint, not a polish item.","origin":"empirical observation across AI coding workflows"}
{"ts":"2026-05-28T15:00:00Z","agent":"rust-implementer","domain":"tdd","lesson":"When the eventual optimal data structure (radix trie) is significantly more complex than a correct-but-suboptimal one (HashMap), ship the simple version with the full test suite first. Tests pin semantics; the data-structure swap is then a pure refactor with bench deltas — the cleanest possible PR.","origin":"RFC-0001 v0.1 implementation"}
{"ts":"2026-05-28T15:05:00Z","agent":"rust-implementer","domain":"path-encoding","lesson":"A materialized-path scheme MUST distinguish 'startsWith(prefix+separator)' from 'startsWith(prefix)' when computing descendants — otherwise `Auth` will incorrectly match `AuthService`. Regression test: `trunk_descendants_must_not_match_prefix_of_sibling_segment_name`.","origin":"classic radix-trie correctness trap"}
{"ts":"2026-05-29T00:00:00Z","agent":"rust-implementer","type":"lesson","domain":"tree-sitter","lesson":"tree-sitter impl_item in Rust grammar uses a \"type\" field (not \"name\") for the implementing type; for generic impls like Vec<T>, the type text includes angle brackets which must be stripped before using as a TrunkPath segment. Use child_by_field_name(\"type\") and split on \"<\" to get the base type name."}
{"ts":"2026-05-30T11:46:00Z","agent":"rust-implementer/ci","type":"lesson","domain":"diagnostic-instrumentation","lesson":"When a test fails on CI but passes locally with no actionable error message (e.g. bare 'assertion failed: status.success()'), the immediate fix is NOT to guess the root cause — it's to add stderr/stdout capture and re-run. One CI cycle with diagnostics beats five cycles of speculation. Pattern: change .status() to .output() and include stderr in the assertion message; the next run will show 'exit=-1073741571' or similar, which is enough to root-cause."}
{"ts":"2026-05-30T11:48:00Z","agent":"rust-implementer/ci","type":"lesson","domain":"merge-strategy","lesson":"For sequential releases (v0.1.4 then v0.1.5), the cleanest gitflow is: cut release/v0.1.4 → ship → back-merge to develop → cut release/v0.1.5 from now-updated develop. Skipping the back-merge step (and cutting v0.1.5 from stale develop) means v0.1.5's CHANGELOG inherits a broken [Unreleased] section that mixes v0.1.4 and v0.1.5 work, requiring manual restructuring at release-cut time."}
{"ts":"2026-05-30T11:50:00Z","agent":"rust-implementer/ci","type":"lesson","domain":"test-portability","lesson":"For tests that assert on file counts or layout, prefer assertions on KNOWN-FILE EXISTENCE over count thresholds. 'store.lookup(\"crates/mycelium-core/src/lib.rs\").is_some()' is robust across all checkout / vendor / package layouts; 'files_indexed >= 100' is fragile and breaks on CI's cargo package layout. The count assertion catches the same class of bug (walker missing a category of files) without being layout-dependent."}
{"ts":"2026-05-30T21:30:00Z","agent":"orchestrator","lesson":"GitHub auto-delete-head-branch setting deletes release/* branches when a PR targeting main is closed (even if not merged). This makes back-merge PRs impossible via GitHub MCP (422 head Code:invalid). Prevention: (a) disable auto-delete for release/* branches in repo settings, OR (b) recreate from tag when needed using `git fetch origin refs/tags/vX.Y.Z:refs/tags/vX.Y.Z && git checkout -b release/vX.Y.Z vX.Y.Z && git push -u origin release/vX.Y.Z`. The tag always survives.","ref":"PR#321 PR#323"}
{"ts":"2026-06-01T10:28:30Z","agent":"rust-implementer","lesson":"In redb file-scoped replacement, stale-node cleanup must be keyed by node-id survival, not by file membership alone. Removing all incoming edges for every old file node deletes valid references owned by other files when a symbol path is stable across edits. Splitting retained old ids from discarded old ids both preserves ownership semantics and reduced the local 10K redb replace benchmark from ~18.4 ms to ~9.70 ms.","ref":"Issue#343,RFC-0100,P2-T05e"}
{"ts":"2026-06-01T10:52:26Z","agent":"rust-implementer","lesson":"For redb-backed storage, idempotent logical operations can still allocate pages if they blindly rewrite identical table entries. Add read-before-write guards for hot unchanged paths before benchmarking memory/page-footprint wins; repeated node upserts are a fast sentinel for this class of write amplification.","ref":"Issue#343,Issue#344,RFC-0100,P2-T05f"}
{"ts":"2026-06-01T11:26:12Z","agent":"rust-implementer","lesson":"For file-scoped redb replacement, compare the persisted file index plus trunk and metadata records before mutating. Edge/idempotence alone is not enough: a no-op replace_file still rewrites owned edges and metadata unless the whole file payload gets a conservative early-return guard.","ref":"Issue#343,Issue#344,RFC-0100,P2-T05g"}
{"ts":"2026-06-01T11:37:57Z","agent":"rust-implementer","lesson":"For redb no-op shortcuts, equality of the ownership index is not enough when public APIs can mutate the underlying adjacency tables independently. A safe early return must validate the actual forward and reverse records it would otherwise repair.","ref":"Issue#343,Issue#344,RFC-0100,P2-T05g"}
{"ts":"2026-06-06T02:30:00Z","agent":"rust-implementer","domain":"packs/python,callgraph,RFC-0113","lesson":"Measuring Mycelium's 'unknown callee tail' (for RFC-0113) has two gotchas. (1) get-all-symbols EXCLUDES stub/file nodes, so bare-stub callees do NOT appear there — use get-callees, where bare callees show up (e.g. main()->['app.py>helper','print','y']; 'print' is a builtin bare stub). (2) packs/python/queries.scm INTENTIONALLY emits Calls edges to ALL identifier arguments (lines ~116-139, @reference.arg_callback): rationale = catch callback-passing like run(callback) so get-isolated-symbols doesn't flag callbacks as dead; documented cost = false callees for data args (foo(x,y,z) records x,y,z as callees). NOT a bug — a deliberate recall>precision trade-off. Implication for RFC-0113: classify_python correctly returns Unknown for x/y/z (not in any table), so the classifier does not worsen the false-positive; but a future refinement could give arg-reaches a weaker edge kind than Calls to de-pollute the call graph (RFC-worthy, not an autonomous change). Controlled repro: def driver(): return foo(x,y,z) -> get-callees driver = [foo,x,y,z].","ref":"RFC-0113,packs/python/queries.scm"}
